.venv/
data/
models/
reports/
__pycache__/
*.pyc
*.db
.optuna*
.env
*.env
secrets.*
# gsp/secrets.py is safe to commit: it only READS the gitignored .env,
# and a clone without it silently loses dashboard enrichment.
!gsp/secrets.py
*.log
# the published dashboard (docs/index.html) IS committed — Pages serves it
